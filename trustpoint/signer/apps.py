"""Apps configuration for the Signer app."""
from django.apps import AppConfig


class SignerConfig(AppConfig):
    """Configuration for the Signer app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'signer'
