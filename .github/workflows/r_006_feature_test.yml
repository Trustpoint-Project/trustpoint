name: R_006

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  backup-and-restore:
    uses: ./.github/workflows/backup-restore.yml
    with:
      artifact_name: 'from_outside.dump.gz'
